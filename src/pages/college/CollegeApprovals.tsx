import CollegeCompanies from "./CollegeCompanies";

// Keeps sidebar route working; the approvals UI is the same as company review.
const CollegeApprovals = () => {
  return <CollegeCompanies />;
};

export default CollegeApprovals;

